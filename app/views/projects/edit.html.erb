<h1>Edit Project</h1>


<%= form_for @project do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit %>

<% end %>

<%= link_to "Create new tasks for this project", new_task_path %>