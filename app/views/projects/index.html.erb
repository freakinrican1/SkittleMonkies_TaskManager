<% if current_user %>

    <h1><%= current_user.username%>'s Projects</h1>

	<% @projects.each do |p| %>
  
    <p><%= link_to(p.title, project_path(p.id))  %></p>

	<% end %>
  
<% else %>

  <p><%= "No Projects" %></p>
  
<% end %>