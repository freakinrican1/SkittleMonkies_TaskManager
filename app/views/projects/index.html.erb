<div id="subhead">
	<h2>Projects</h2>
</div>

<div id="feature">
  
<h4><%= current_user.username.capitalize %>'s Projects</h4>
<% if @projects.nil? %>

  <p><%= "No Projects" %></p>
  
<% else %>

	<% @projects.each do |p| %>
  
    <p><%= link_to(p.title, project_path(p.title))  %></p>

	<% end %>
  
<% end %>

<%= link_to("Create New Project", new_project_path) %>

</div>